```css
    @keyframes name {
        keyframes-selector {css-styles;}
        keyframes-selector {css-styles;}
        ...
    }
```
keyframes-selector	Required. Points along the animation sequence.
Legal values:  
0-100%  
from (same as 0%)  
to (same as 100%)  
